import { colors } from "../styles/colors";
const pins = {
  startPins: 1,
  pinSize: 4,
  pinGap: 40,
};

const ball = {
  ballSize: 5.7,
};

const engine = {
  engineGravity: 1.0,
};

const world = {
  width: 800,
  height: 800,
};

export const config = {
  pins,
  ball,
  engine,
  world,
  colors,
};
